[tool.uv]
package = false

[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[dependency-groups]
dev = [
    "pytest>=8.3.5",
]

[tool.uv.workspace]
members = [
  "packages/br_tester",
  "packages/br_common",
  ]

[tool.uv.sources]
br_tester = { workspace = true }
br_common = { workspace = true }

[tool.ruff]
line-length = 119
select = ["E", "F", "I"]
target-version = "py312"

[tool.pytest.ini_options]
pythonpath = [
  "packages/br_tester/src",
  "packages/br_common/src"
]

